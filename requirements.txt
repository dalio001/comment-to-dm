# Lower bounds chosen so the app installs cleanly on Python 3.12 (Docker) through
# 3.14 (prebuilt wheels available). Pip resolves the latest compatible release.
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
python-dotenv>=1.0
jinja2>=3.1
requests>=2.32
python-multipart>=0.0.20
pydantic>=2.11
psycopg2-binary>=2.9  # PostgreSQL driver (production persistence)
apscheduler>=3.10     # in-app daily auto-arm scheduler
tzdata>=2024.1        # IANA timezone data (Docker slim images lack system tz)
